# 重构后逐项测试方案

日期：2026-08-27。板卡：STM32F103RCT6 电机控制板。

**本次范围：保持既有功能，灯板只搭空接口。编译/主机模拟通过不等于实机通过；用户于 2026-08-31 报告既有功能实机测试暂时无误，并确认更换器件后的 24 V 抱闸输出正常。光电限位开关安排在下一聊天中逐项测试。**

**2026-08-31 更新：**限位传感器已改用 EE-SX951P-R 的 PNP OUT1，当前固件按 MCU 高电平有效采样；本轮上电输入测试请使用 [`doc/limit_input_test_plan_20260831.md`](limit_input_test_plan_20260831.md)。下方 L01～L05 是此前低有效方案的历史测试记录，暂不按其内容执行限位停机测试。

**2026-09-01 更新：**九路限位已切换为双边沿 EXTI；遮光上升沿在 ISR 中立即停止 X 轴 STEP/驱动，停机状态和串口应答在主循环完成。请使用限位输入测试清单中的 EXTI 与限位停机部分进行验证。

**2026-09-01 实测更新：**用户已确认 X 轴连续运行正常；运动中遮挡 X 轴限位可停止电机，并回传 `AA AA 02 FF 0B 00 00 00 00 00 00 00 55 55`。本结果只覆盖 X 轴限位中止链路；其他限位通道、STEP停止波形、XEN保持电平和掉电/断线场景仍需按专用清单记录。

## 本次已执行的验证记录

| 项目 | 2026-08-27 结果 |
| --- | --- |
| 基线 Keil 6.24 全量编译 | 0 错误、0 警告；Code 15468、RO 408、RW 12、ZI 2396 字节。 |
| 重构后 Keil 6.24 全量编译 | 0 错误、0 警告；Code 15054、RO 474、RW 12、ZI 2492 字节。 |
| 结构/源代码对照 | 通过：ISR 原文、初始化函数体、时钟、TIM2 中断设置顺序、TMC 寄存器传输、厂商源码、`.ioc`、编译选项及模块依赖检查。 |
| 默认配置主机 C 差分测试 | **49 组通过**，包含确定性噪声输入、边界参数、运动波形计算、连包/分包、停止/限位优先顺序、模拟故障与灯板空接口。 |
| 原自检配置主机 C 差分测试 | **2 组通过**；只在主机生成的测试源码中切换宏，没有修改固件默认配置。 |
| 另外两个非默认配置 | `static_limits` 和 `continuous_disabled` 各 2 组**未执行完成**：基线测试 EXE 启动时 Windows 返回 WinError 5，随后文件不可用。未修改系统安全设置，不计为通过。 |
| 实机烧录/测量 | 本轮未由助手执行；用户于 2026-08-31 报告既有功能测试暂时无误，并确认更换器件后的 24 V 抱闸输出正常。光电限位开关留待下一聊天测试。 |

基线 HEX SHA-256：`E5E726FA41CCF6CA11C0C431C41E84EFBB416AA5C0546432E8A021AF8A50B1E2`。

重构 HEX SHA-256：`49B143DC49089FB5CFDB10E6AA2A3122175392F1CCA052AB8855E12E98599A9E`。

证据：`tmp/refactor_baseline_20260827/baseline_build.log`、`tmp/refactor_final_build.log`、`tmp/refactor_host_tests/report.json`（51 条已通过记录）。

ISR 源码未变，但编译后的回调代码节由 532 字节变为 500 字节。因此“逐周期响应时间相同”**尚未验证**，必须执行 T01～T03，不能只凭编译通过判定时序通过。

## 1. 测试前准备

1. 使用同一块板、同一电源、同一电机和同一串口工具，先记录重构前基线，再测试新代码。不要用较早的发布版代替本次基线：工作区已有新的加减速逻辑。
2. 基线完整副本：`tmp/refactor_baseline_20260827/MotorContrl/`。新工程：`MotorContrl/MDK-ARM/MotorContrl.uvprojx`。两者都用 Arm Compiler 6.24 原配置编译。
3. 基线 HEX：`tmp/refactor_baseline_20260827/MotorContrl/MDK-ARM/MotorContrl/MotorContrl.hex`；重构 HEX：`MotorContrl/MDK-ARM/MotorContrl/MotorContrl.hex`。均属于本次测试构建，不是已验收发布文件。
4. CN4 接主机串口，115200、8 数据位、无校验、1 停止位，以 HEX 方式发送，不追加 CR/LF。CN6 保留，不接入主控业务。
5. 电机先脱离危险机构、空载短时测试。现有记录中抱闸器件有硬件问题：PB13/CN5 先测空载电平，不接抱闸负载。限位接线未确认前不得依赖软件限位进行机构运动。
6. 准备万用表、逻辑分析仪/示波器；测量地接板上 GND。限位需经过正确输入链路或已确认的 3.3 V 测试点触发，不得把 24 V 直接接 MCU GPIO。
7. 记录测试日期、HEX SHA-256、供电电压、串口日志和波形文件。任一不一致先记录，不通过修改参数来掩盖差异。

## 2. 不接硬件的检查

| 编号 | 检查 | 判定 |
| --- | --- | --- |
| B01 | Keil Rebuild 基线和新工程 | 均为 0 错误、0 警告；确认生成的是本次 HEX。 |
| B02 | ISR、系统启动、`.ioc`、HAL/CMSIS 对比 | `stm32f1xx_it.c` 不变；TIM2 回调及三个计算函数原文不变；厂商源码不变。 |
| B03 | 优先级与定时器 | TIM2 优先级仍 2/0；SysTick 仍 15，分组仍为原配置；不新增 UART/限位中断。TIM2/3/4 参数保持原值。 |
| B04 | 主机差分测试 | 原、新 C 代码对同一输入的硬件调用顺序和全部旧状态值一致。包含故障路径、分包/连包、自检模式、灯板占位。 |
| B05 | 工程目录和依赖 | `Inc/Src/Drivers/APPs`；新文件列入 Keil；业务模块没有直接相互调用或 extern 业务变量。 |

可运行：

```powershell
python tests/check_refactor.py --structure-only
python tests/check_refactor.py --vcvars "D:\APP\Visual Studio 2026\install\VC\Auxiliary\Build\vcvars64.bat"
```

仅重跑本机目前可执行的两种配置：`python tests/check_refactor.py --modes default selftest`。完整命令仍保留另外两个配置；如果系统拒绝运行，记录阻塞，不应为通过测试而关闭安全保护。

若 Python 不在 PATH，使用本机可用 Python 3 的绝对路径。测试夹具为重构前工作区源码，测试程序在 `tmp/refactor_host_tests/` 编译运行；不会打开串口、烧录或运行电机。明细结果写入该目录 `report.json`。

## 3. 上电、基本通信与其他输出

每项在“基线/新版”栏分别填通过或失败，不要只测新版。

| 编号 | 操作 | 预期 | 基线/新版 |
| --- | --- | --- | --- |
| P01 | 上电，暂不发命令 | XYZ EN 默认禁用；不新增 STEP；PB12/PB13 初始关闭；与基线启动波形一致。 | □ / □ |
| P02 | 空闲观察 PC14 LED ≥5 s | 每约 500 ms 翻转一次；阻塞业务期间的变化与基线一致。 | □ / □ |
| P03 | CN4 发链路测试帧 10 次 | 每帧一个准确回包，无电机动作。 | □ / □ |
| P04 | 测试帧分两段发送，再连续发两帧 | 半帧不回；收齐回一个；连发两帧回两个，保持顺序。 | □ / □ |
| P05 | 发非 `55` 噪声、错误帧尾、未知命令，再发合法测试帧 | 无误动作；恢复行为与基线一致；不擅自要求原实现没有的超时重同步。 | □ / □ |
| P06 | CN6 发送同样测试帧 | 不作为主业务入口，不触发运动；CN4 功能仍正常。 | □ / □ |
| P07 | `03 00` 开/关各 5 次，测 PB12/CN1 | PB12 高时输出开启，低时关闭；合法请求各一个原样数据回包。 | □ / □ |
| P08 | 空载 `04 00` 开/关各 5 次，测 PB13/CN5 | PB13 高对应抱闸释放控制，低对应关闭；无新增电机动作。不得接有问题的抱闸。 | □ / □ |
| P09 | 继电器/抱闸发非法动作、非零保留数据、错误帧尾 | 不改变原输出，不发送成功应答；原错误计数增加。原实现不发送这些辅助命令的错误应答。 | □ / □ |

可直接发送的完整帧：

| 功能 | 请求（14 字节） |
| --- | --- |
| 链路测试 | `55 55 01 00 00 00 00 00 00 00 00 00 AA AA` |
| 继电器开 | `55 55 03 00 01 00 00 00 00 00----------------- 00 00 AA AA` |
| 继电器关 | `55 55 03 00 02 00 00 00 00 00 00 00 AA AA` |
| 抱闸释放控制（空载） | `55 55 04 00 01 00 00 00 00 00 00 00 AA AA` |
| 抱闸关闭控制（空载） | `55 55 04 00 02 00 00 00 00 00 00 00 AA AA` |

以上合法回包把请求帧头 `55 55` 换为 `AA AA`、帧尾 `AA AA` 换为 `55 55`，中间字节不变。

## 4. X 轴有限运动和加减速

**按实际代码，速度和距离为大端字节序**，保留现状；本次没有按协议文档中“全部小端”的表述改代码。速度单位 STEP/s，距离单位 STEP 脉冲数。

| 编号 | 参数/请求 | 预期 | 基线/新版 |
| --- | --- | --- | --- |
| M01 | 1000 Hz、1 STEP：`55 55 02 00 01 00 03 E8 00 00 00 01 AA AA` | 先接收应答，准备后 1 个 STEP，再完成应答；无多余脉冲。 | □ / □ |
| M02 | 1000 Hz、1000 STEP：`55 55 02 00 01 00 03 E8 00 00 03 E8 AA AA` | 1000 个脉冲；同方向 MSCNT 模 1024 校验一致。 | □ / □ |
| M03 | M02 第 6 字节改为 `01`，反向 | DIR 反向，脉冲数仍 1000；完成应答中的方向也为 `01`。 | □ / □ |
| M04 | 3000 Hz、3000 STEP：`55 55 02 00 01 00 0B B8 00 00 0B B8 AA AA` | 与基线相同的加速/匀速/减速和脉冲数。 | □ / □ |
| M05 | 10000 Hz、1000 STEP：`55 55 02 00 01 00 27 10 00 00 03 E8 AA AA` | 短行程不必达到目标最高速；不能把原加减速误改为全程 10 kHz。 | □ / □ |
| M06 | 10000 Hz、12000 STEP：`55 55 02 00 01 00 27 10 00 00 2E E0 AA AA` | 12000 脉冲；观察最高速段，无漏脉冲/重复脉冲/异常周期。 | □ / □ |
| M07 | 同一运动完成后再执行，共 10 次 | 每次均恢复 STEP 复用模式并正常结束；没有第二次不动的问题。 | □ / □ |
| M08 | 第一条运动运行中再发合法启动 | 第二条返回忙错误 `07`，不得覆盖当前目标或追加运动。 | □ / □ |

正常接收：`AA AA 02 00 [原请求8字节数据] 55 55`。

正常完成：`AA AA 02 01 [已接受目标8字节数据] 55 55`。不要把接收应答当作电机已经启动；旧准备流程仍有 100+200+1 ms 阻塞等待。

## 5. 运动校验、停止、查询、限位

| 编号 | 操作 | 预期 | 基线/新版 |
| --- | --- | --- | --- |
| E01 | 运动帧尾错误 | `02 FF`，错误码 `01`，不开始新运动。 | □ / □ |
| E02 | 轴号改为 `02` 或 `03` | 错误码 `02`；没有新增 Y/Z 轴业务。 | □ / □ |
| E03 | 方向改为 `02` | 错误码 `03`。 | □ / □ |
| E04 | 速度 999=`03 E7` 或 10001=`27 11` | 错误码 `04`。 | □ / □ |
| E05 | 距离 0 或 12001=`00 00 2E E1` | 错误码 `05`。 | □ / □ |
| E06 | 1000 Hz、5001 STEP=`00 00 13 89` | 错误码 `06`。旧判断仍为 `distance > speed*5`，未按加减速实际时长重算。 | □ / □ |
| S01 | 空闲发停止：`55 55 02 02 01 00 00 00 00 00 00 00 AA AA` | 错误码 `0A`。 | □ / □ |
| S02 | 长一些的有限运动中发 S01 | 停 PWM、STEP 低、保持 X 驱动使能，然后返回 `02 02`；无正常完成假回包。 | □ / □ |
| S03 | 查询：`55 55 02 03 00 00 00 00 00 00 00 00 AA AA` | `AA AA 02 03 [轴号][状态][速度2字节][已发脉冲4字节] 55 55`。 | □ / □ |
| S04 | 空载短时持续运行：`55 55 02 F0 01 00 03 E8 00 00 00 00 AA AA`，随后立即按需停止 | 保留旧验证命令；持续运行不自动按有限距离结束；S01 能停止。勿依赖旧“5秒”校验自动停此命令。 | □ / □ |
| S05 | 一次发送“启动帧 + 查询帧 + 停止帧” | RX 缓冲未清空前逐帧处理；停止可取消 pending 启动；回包顺序与基线相同。 | □ / □ |
| L01 | 九路限位逐一触发与释放 | 10 ms 调试采样掩码位对应下表，低有效，没有新增去抖或 EXTI。 | □ / □ |
| L02 | X 任一路限位预先触发，再启动 | 接收应答后启动检查报 `0B`，不发 STEP。 | □ / □ |
| L03 | X 运动中分别触发 PC6/PB15/PB14 | 停 PWM、STEP 低、保持 X 驱动使能；回 `02 FF` 错误码 `0B`。 | □ / □ |
| L04 | 有条件时，在准备等待阶段触发 X 限位 | 原三次即时检查位置保持；不得等到缓存采样后才决定。 | □ / □ |
| L05 | 运动最后一个脉冲附近触发限位或发停止 | 核对旧优先级：限位检查、停止先于正常完成处理；对比两版回包与状态。 | □ / □ |

运动错误帧格式：`AA AA 02 FF [错误码] 00 00 00 00 00 00 00 55 55`。

状态值：0 空闲、1 待启动、2 运行、3 完成、4 错误、5 主机停止、6 限位停止。

| 限位 | GPIO | 掩码 |
| --- | --- | --- |
| X-L / X-H / X-R | PC6 / PB15 / PB14 | `0001` / `0002` / `0004` |
| Y-L / Y-H / Y-R | PC9 / PC8 / PC7 | `0008` / `0010` / `0020` |
| Z-L / Z-H / Z-R | PA12 / PA11 / PA10 | `0040` / `0080` / `0100` |

## 6. 时序、异常与长一点的重复测试

| 编号 | 操作 | 判定 | 基线/新版 |
| --- | --- | --- | --- |
| T01 | 同时采 CN4 TX、X EN、DIR、STEP，记录 M02/M06 | 接收回包、100 ms 清错等待、使能后 200 ms 等待、DIR 后 1 ms 等待、STEP 开始/结束顺序一致。 | □ / □ |
| T02 | 最高速时持续发查询/合法辅助命令，观察 STEP | 脉冲总数、周期序列、占空比行为对比基线，无因封装产生的丢步或突变。 | □ / □ |
| T03 | 空闲串口与连续串口输入两种情况下测限位→停止延迟 | 保留原轮询影响；记录两版最大值。任何超出测量误差的差异先确认，不默认判定时序通过。 | □ / □ |
| T04 | 连续重复短运动、查询、停止、继电器开关至少 50 轮 | 无状态卡死、无应答丢失；异常计数与基线输入条件一致。 | □ / □ |
| F01 | 经确认的可控 SPI 故障条件下尝试启动，不带危险负载 | 返回 `08`；启动安全行为与基线相同。不要带电拔电机或短接总线制造故障。 | □ / □ |
| F02 | 主机工具中断/重连、输入错误字节，再发送合法帧 | 恢复与基线一致；不把原 UART 无接收超时设计当成此次新功能。 | □ / □ |

定时器启动失败 `09`、UART 发送失败、启动期间故障等难以安全制造的情形，由主机模拟覆盖；未做实机故障注入的项目要标记“仅模拟”。不修改当前默认宏去自动启动旧自检；旧自检只在隔离的主机测试构建中启用验证。

## 7. 灯板占位验收（本次）

| 编号 | 操作 | 本次应出现的结果 | 新版 |
| --- | --- | --- | --- |
| A01 | 检查灯板/DAC 空接口和 Keil 编译 | `app_light.c/.h`、`mcp4728.c/.h` 已纳入；公共函数可供后续填写。 | □ |
| A02 | 上电运行、发送灯板帧 `55 55 05 00 01 00 01 00 00 00 00 00 AA AA` | 当前仍是不支持命令：不发成功回包，不新增 I2C 事务，不影响既有输出。 | □ |
| A03 | 逻辑分析仪观察 PB8/PB9 | 除既有初始化电平行为外，没有灯板软件发起的 I2C 通信。 | □ |
| A04 | 检查 CN10 电压 | 只记录，不要求 0 V 或 5 V；本次没有写 DAC，其电压可能由 EEPROM 上电配置决定。 | □ |

**后续才测试**：实际 I2C 电平/ACK、LDAC、DAC 参考源、通道 1～4 对应 A～D/CN10-1～4、开关输出电压、掉电恢复。SCL/SDA 的 3.3 V 上拉风险仍未解决；这不阻止本次空接口测试，但不能据此宣称灯板硬件通过。

## 8. 调试变量位置和记录模板

原 ISR 变量如 `serial_motion_pulses_done`、`serial_motion_active`、`serial_motion_current_speed_hz` 保持原名称。其他调试数据改为调度器的私有上下文成员，可在 `app_scheduler.c` 作用域查看：

| 原变量 | 新位置示例 |
| --- | --- |
| `serial_motion_busy` | `runtime.motion.serial_motion_busy` |
| `serial_test_frame_error_count` | `runtime.protocol.serial_test_frame_error_count` |
| `serial_relay_state` | `runtime.aux.serial_relay_state` |
| `limit_active_mask` | `runtime.limits.limit_active_mask` |
| `x_tmc5160_spi_ok` | `runtime.tmc.x_tmc5160_spi_ok` |

不要把“旧 Watch 表达式找不到符号”当成逻辑故障；改用新位置。原 build marker `0x20260826` 故意不改，识别两版请用 HEX 哈希，不能只看该变量。

```text
测试编号：
测试日期 / 板卡编号：
基线 HEX SHA-256：
新版 HEX SHA-256：
输入帧 / 接线 / 参数：
基线结果：
新版结果：
波形或串口日志路径：
结论：通过 / 失败 / 未测试 / 仅模拟
异常说明：
```

全部适用项完成且差异确认后，才能判定现有功能与时序验收通过。本次不自动烧录、不提交发布标签、不把未上板的 HEX 放入已验证发布目录。
