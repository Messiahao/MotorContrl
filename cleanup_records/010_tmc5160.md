### 模块：`MotorContrl/Drivers/Board/tmc5160.c` + `MotorContrl/Inc/tmc5160.h`

- 扫描时间：2026-09-02 17:20:05 +08:00
- 当前条件编译配置：`USE_HAL_DRIVER`、`STM32F103xE`；未定义 `DEBUG`、`TEST`、`TRACE`、`LOG`、`DBG`。模块自身无功能条件编译。
- 静态扫描：
  - 全局/静态变量：无。
  - 文件静态函数：`Tmc5160_WriteChipSelect`。
  - 公开函数：`BspTmc5160_WriteEnable`、`BspTmc5160_WriteRegister`、`BspTmc5160_ReadRegister`、`BspTmc5160_Init`。
  - 公开类型：`TMC5160_HandleTypeDef`；四个 CS/EN 端口与引脚字段均被 GPIO 路径读取。
  - 局部变量：写寄存器的 `tx[5]`；读寄存器的 `tx[5]`、`rx[5]`、`value`；均有完整读写路径。
  - 宏：头文件保护 `__TMC5160_H`；帧、写位、延时、版本、MSCNT 和字节位移宏；寄存器地址宏；GCONF/GSTAT/IHOLD_IRUN/CHOPCONF 字段及初始化宏；电流、全局缩放与掉电延时宏。
  - 调试属性命名：无调试变量、测试缓冲、打印函数或未使用计数器。
- 引用分析：
  - 片选函数在每次 SPI 帧前后调用；低有效 CS 与三轴独立片选硬件要求一致。
  - 使能、寄存器读写接口被运动控制和生产自检反复调用；局部 SPI 数组及返回值均被读取。
  - `BspTmc5160_Init` 当前无调用，调度器明确保留“不执行完整启用序列”的基线行为；该函数仍是公开驱动初始化接口，不能删除。
  - `TMC5160_SHORT_CONF`、`TMC5160_DRV_CONF`、`TMC5160_PWMCONF`、`TMC5160_GCONF_EN_PWM_MODE`、两组预设电流宏当前无固件调用，但属于公开寄存器/配置接口，按对外兼容要求保留。
  - `tests/check_refactor.py` 对寄存器读、写、初始化三个函数与重构前实现执行 token 级冻结比较，不能合并帧、删除流水读或改动寄存器顺序。
  - 未发现只写不读、仅读不写且非硬件/API 对象的变量。
- 删除的变量/宏/函数：无。
- 合并/简化的逻辑：无。片选包装被四次复用；读寄存器必须保持两帧流水事务；初始化寄存器顺序和延时属于硬件时序且受回归冻结。
- 保留但标记为“待确认”的项：无。无内部调用的初始化函数和配置宏均按明确的公开接口兼容要求保留。
- 修改后代码行数变化：0 行（`tmc5160.c` 原 191 行 → 现 191 行；`tmc5160.h` 原 203 行 → 现 203 行）。
- 编译检查：通过。Keil Arm Compiler 6.24 构建 0 errors、0 warnings。
- 结构/行为检查：TMC5160 冻结函数检查通过；完整冻结结构检查通过；行为脚本随后仍在既有 `default case 0` 初始化轨迹差异处失败（旧 `661db29559361e9d/19`，新 `ef9af7514dad37d3/36`），本模块未产生新差异。
- 资源结果：链接映像保持 `Code=18674`、`RO-data=650`、`RW-data=12`、`ZI-data=2700`。
- 硬件/时序检查：保持 SPI1 PA5/PA6/PA7、X/Y/Z 独立低有效 CS、低有效 EN、40 位帧、两帧流水读取、`HAL_MAX_DELAY`、10 ms 初始化延时及原寄存器写入顺序。
- 待办事项：
  - [ ] TMC5160 电流、斩波和微步参数仍需结合电机与实机测量确认；本次不改寄存器值。

#### Diff

```diff
# 无源码改动
```
