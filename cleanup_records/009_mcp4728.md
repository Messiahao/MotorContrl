### 模块：`MotorContrl/Drivers/Board/mcp4728.c` + `MotorContrl/Inc/mcp4728.h`

- 扫描时间：2026-09-02 13:44:51 +08:00
- 当前条件编译配置：Keil 目标定义 `USE_HAL_DRIVER`、`STM32F103xE`；未定义 `DEBUG`、`TEST`、`TRACE`、`LOG`、`DBG`。模块自身只有头文件保护和 `__cplusplus` 条件，C++ 链接包装因包含公开函数声明而保留。
- 静态扫描：
  - 全局变量、文件静态变量、静态函数：无。
  - 公开函数：`BspMcp4728_Init`、`BspMcp4728_Read`、`BspMcp4728_Write`；均在 `mcp4728.h` 声明。
  - 局部变量：`BspMcp4728_Write` 的三字节栈数组 `data`；每个元素均在 I²C 发送前赋值并由 `BspI2c_Write` 读取。
  - 宏：头文件保护宏 `MOTOR_MCP4728_H`；通道宏 `BSP_MCP4728_CHANNEL_A/B/C/D`；码值宏 `BSP_MCP4728_CODE_OFF`、`BSP_MCP4728_CODE_FULL_SCALE`；通信宏 `BSP_MCP4728_I2C_ADDRESS`、`BSP_MCP4728_I2C_TIMEOUT_MS`；状态宏 `BSP_MCP4728_NOT_IMPLEMENTED`。
  - 调试属性命名：无 `DEBUG_`、`dbg_`、`debug_`、`test_`、`tmp_`、打印缓冲或调试计数器。
- 引用分析：
  - `BspMcp4728_Init` 为空实现，用于保持上电恢复的 DAC 值不变；公开接口保留。
  - `BspMcp4728_Read` 当前无工程内调用，但属于公开接口；其返回“未实现”且不改写调用者缓冲区的行为由现有架构约定，不能删除或伪造读值。
  - `BspMcp4728_Write` 由 `AppLight_Process` 调用；通道及 12 位码值在发送前校验，I²C 地址、长度、数据帧和超时由主机回归覆盖。
  - 通道 A/B/C 宏当前仅在公开头文件定义，通道 D 用于上界校验；四个宏共同构成公开通道编号接口，按对外兼容要求保留。
  - `BSP_MCP4728_CODE_OFF/FULL_SCALE` 被灯光应用使用；地址、超时及“未实现”宏被驱动使用。
  - 无只写不读、只读不写且非接口参数的变量。
- 删除的变量/宏/函数：无。
- 合并/简化的逻辑：
  - `BspMcp4728_Write` 已先拒绝大于 4095 的码值，因此 `code >> 8` 必然处于 `0x00..0x0F`；删除重复的 `& 0x0F`，保持 Multi-Write 高四位完全一致。
- 保留但标记为“待确认”的项：无。无内部调用的读取函数和部分通道宏属于明确要求保留的公开接口，不作为待删项。
- 修改后代码行数变化：0 行（`mcp4728.c` 原 33 行 → 现 33 行；`mcp4728.h` 原 28 行 → 现 28 行）。
- 编译检查：通过。Keil Arm Compiler 6.24 重新编译 `mcp4728.c`，构建 0 errors、0 warnings。
- 结构/行为检查：全部 4096 个有效 DAC 码值的旧/新高字节表达式穷举比较无差异；冻结结构检查通过；`tests/check_refactor.py` 随后仍在本轮处理前已存在的 `default case 0` 初始化轨迹差异处失败（旧 `661db29559361e9d/19`，新 `ef9af7514dad37d3/36`），本模块未产生新差异。
- 资源结果：链接映像保持 `Code=18674`、`RO-data=650`、`RW-data=12`、`ZI-data=2700`。
- 硬件/协议检查：保持 MCP4728 地址 `0x60`（HAL 左移地址 `0xC0`）、Multi-Write 命令、VDD 参考、正常功耗、1 倍增益、12 位码值及 100 ms 超时；保持 PB8/PB9 I²C1、PC13 LDAC 低电平即时更新，不触碰 PC15 RDY。SCL/SDA 电平兼容问题仍属未完成的硬件验证，不在本次精简中改动。
- 待办事项：
  - [ ] 改板后继续实机验证 MCP4728 ACK、四路映射、输出电压及 SCL/SDA 高电平裕量；本次仅验证代码等价性。

#### Diff

```diff
-  data[1] = (uint8_t)((code >> 8) & 0x0FU);
+  data[1] = (uint8_t)(code >> 8);
```

后续关联记录：调度器处理时移除了对空实现 `BspMcp4728_Init` 的内部调用，公开接口和上电 DAC 保持行为不变，详见 `cleanup_records/018_app_scheduler.md`。
