### 模块：`MotorContrl/Drivers/Board/tim.c` + `MotorContrl/Inc/tim.h`

- 扫描时间：2026-09-02 13:34:23 +08:00
- 当前条件编译配置：`USE_HAL_DRIVER`、`STM32F103xE`；未定义 `DEBUG`、`TEST`、`TRACE`、`LOG`、`DBG`。头文件的 `__cplusplus` 包装因包含函数和外部变量声明而保留。
- 静态扫描：
  - 全局变量：`htim2`、`htim3`、`htim4`；均在 `tim.h` 以 `extern` 声明，供 HAL 中断入口和轴定时器映射使用。
  - 文件静态函数：`MX_TIM2_Init`、`MX_TIM3_Init`、`MX_TIM4_Init`、`BspTim_GetAxis`。
  - 函数内静态变量：`BspTim_GetAxis` 中的 `static const BspTimMotionAxis axes[3]`，保存 X/Y/Z 的句柄、通道、比较标志、比较中断和 IRQ 映射。
  - 外部 HAL 函数：`HAL_TIM_PWM_MspInit`、`HAL_TIM_MspPostInit`、`HAL_TIM_PWM_MspDeInit`。
  - 公开初始化函数：`BspTim2_Init`、`BspTim3_Init`、`BspTim4_Init`。
  - 公开轴接口：`BspTim_IsAxisTimer`、`BspTim_WriteAxisPeriod`、`BspTim_WriteAxisSetupInterrupt`、`BspTim_WriteAxisStart`、`BspTim_WriteAxisStop`、`BspTim_WriteAxisDisableUpdate`、`BspTim_WriteAxisEmergencyStop`。
  - 公开 X 轴兼容接口：`BspTim_WriteXSetupInterrupt`、`BspTim_WriteXStart`、`BspTim_WriteXStop`、`BspTim_WriteXDisableUpdate`、`BspTim_WriteXEmergencyStop`。
  - 公开类型：`BspTimMotionAxis`；字段 `handle`、`channel`、`compare_flag`、`compare_interrupt`、`irqn` 均被读取。
  - 局部变量：三个 MX 初始化函数中的 `sMasterConfig`、`sConfigOC`，MSP 后初始化中的 `GPIO_InitStruct`，以及各轴接口中的 `axis_timer`；均有完整读写路径。
  - 宏：头文件保护宏 `__TIM_H__`，以及 `BSP_TIM_PRESCALER`、`BSP_TIM2_INITIAL_PERIOD`、`BSP_TIM34_INITIAL_PERIOD`、`BSP_TIM_INITIAL_PULSE`、`BSP_TIM2_IRQ_PRIORITY`、`BSP_TIM2_IRQ_SUBPRIORITY`。
  - 调试属性命名：无 `DEBUG_`、`dbg_`、`debug_`、`test_`、`tmp_` 符号。
- 引用分析：
  - `htim2/3/4` 分别由 TIM2/3/4 IRQ 调用 `HAL_TIM_IRQHandler`，并被轴映射表使用，不可改为未暴露状态。
  - 三个 `BspTim*_Init` 均由应用调度器调用；对应三个 MX 初始化函数是其唯一直接初始化入口。
  - 轴周期、定时器识别、启动、停止、中断配置、禁止更新和急停接口均由 `main.c`、运动模块或限位模块调用。
  - `BspTim_WriteXStart`、`BspTim_WriteXStop` 被生产自检使用；`BspTim_WriteXSetupInterrupt`、`BspTim_WriteXDisableUpdate`、`BspTim_WriteXEmergencyStop` 当前无固件调用。
  - `tests/check_refactor.py` 对三个 MX 初始化函数和三个 HAL MSP 函数执行 token 级冻结比较，并检查 TIM2/3/4 中断及项目配置，不能合并或重写这些函数。
  - 所有配置宏均至少被一个初始化或 IRQ 配置路径引用；无只写不读、只读不写且非 HAL/寄存器对象的变量。
- 删除的变量/宏/函数：无。
- 合并/简化的逻辑：无。三轴映射已经集中在单个只读表中；继续内联会重复硬件参数，合并 MX 包装会破坏冻结结构。
- 保留但标记为“待确认”的项：
  - `BspTim_WriteXSetupInterrupt`、`BspTim_WriteXDisableUpdate`、`BspTim_WriteXEmergencyStop`：工程内无调用，但属于公开兼容接口，不能在保持对外接口一致的前提下删除。
  - `BspTimMotionAxis` 当前仅由 `tim.c` 使用，但定义在公开头文件中；移动到源文件会改变头文件接口，暂不处理。
- 修改后代码行数变化：0 行（原 533 行 → 现 533 行）。
- 编译检查：通过。Keil Arm Compiler 6.24 构建 0 errors、0 warnings。
- 结构/行为检查：冻结结构检查通过；`tests/check_refactor.py` 随后仍在本轮处理前已存在的 `default case 0` 行为差异处失败。
- 资源结果：当前链接映像保持 `Code=18674`、`RO-data=650`、`RW-data=12`、`ZI-data=2700`。
- 时序检查：保持 72 MHz 定时器时钟、预分频 71、TIM2 初始周期 99、TIM3/4 初始周期 999、初始脉宽 50，以及 X=TIM2_CH2/PA1、Y=TIM3_CH4/PB1、Z=TIM4_CH1/PB6 的映射；IRQ 清标志、使能顺序和优先级均未改变。
- 待办事项：
  - [ ] 最终确认工程外是否依赖三个无内部调用的 X 轴兼容接口及公开轴描述类型。

#### Diff

```diff
# 无源码改动
```
