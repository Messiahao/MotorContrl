### 模块：`MotorContrl/Drivers/Board/spi.c` + `MotorContrl/Inc/spi.h`

- 扫描时间：2026-09-02 13:16:30 +08:00
- 最终复核时间：2026-09-02 13:20:06 +08:00
- 当前条件编译配置：`USE_HAL_DRIVER`、`STM32F103xE`；未定义调试条件。
- 静态扫描：
  - 文件静态变量 `hspi1`：SPI1 HAL 句柄，被初始化、发送和全双工收发路径共同使用。
  - 全局变量：无；`hspi1` 未通过 `extern` 暴露。
  - 静态函数 `MX_SPI1_Init`：由 `BspSpi_Init` 调用。
  - 外部函数：`HAL_SPI_MspInit`、`HAL_SPI_MspDeInit`、`BspSpi_Init`、`BspSpi_Write`、`BspSpi_ReadWrite`。
  - 局部变量：`HAL_SPI_MspInit` 内的 `GPIO_InitStruct`，用于 PA5/PA7 复用推挽及 PA6 上拉输入配置。
  - 宏：头文件保护宏 `__SPI_H__` 和 `BSP_SPI_CRC_POLYNOMIAL`。
  - 调试属性命名：无 `DEBUG_`、`dbg_`、`debug_`、`test_`、`tmp_` 符号。
- 引用分析：
  - `BspSpi_Init` 由应用调度器调用。
  - `BspSpi_Write`、`BspSpi_ReadWrite` 均由 TMC5160 驱动调用。
  - `HAL_SPI_MspInit`、`HAL_SPI_MspDeInit` 由 HAL 弱回调机制调用，必须保留。
  - `hspi1` 所有访问均限于本文件，没有只写不读或只读不写问题。
  - `BSP_SPI_CRC_POLYNOMIAL` 参与 HAL 句柄初始化；即使当前关闭 CRC，也保留初始化状态。
  - `tests/check_refactor.py` 对 `MX_SPI1_Init` 函数体执行 token 级冻结比较。
- 删除的变量/宏/函数：无。
- 合并/简化的逻辑：无。曾尝试把 `MX_SPI1_Init` 主体并入 `BspSpi_Init`，但破坏结构回归检查，已逐字节恢复。
- 保留但标记为“待确认”的项：
  - `BspSpi_Init -> MX_SPI1_Init` 包装：运行上可合并，但当前 `MX_SPI1_Init` 是冻结结构的一部分。
- 修改后代码行数变化：0 行（原 193 行 → 现 193 行）。
- 编译检查：通过。Keil Arm Compiler 6.24 最终全量构建 0 errors、0 warnings。
- 结构检查：通过；候选合并回退后文件与处理前快照 SHA-256 一致。
- 资源结果：`Code=18674`、`RO-data=650`、`RW-data=12`、`ZI-data=2700`。
- 硬件检查：SPI1 仍使用 PA5/SCK、PA6/MISO、PA7/MOSI；保持主机、双线、8 位、CPOL=高、第二边沿采样、软件 NSS、APB 分频 32、MSB first，三轴片选逻辑未改动。
- 待办事项：无。

#### Diff

```diff
# 无最终源码改动；候选初始化合并因结构回归约束已恢复。
```
