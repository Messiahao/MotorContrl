### 模块：`MotorContrl/Drivers/Board/led.c` + `MotorContrl/Inc/led.h`

- 扫描时间：2026-09-02 12:42:18 +08:00
- 当前条件编译配置：Keil 工程定义 `USE_HAL_DRIVER`、`STM32F103xE`；未定义 `DEBUG`、`TEST`、`TRACE`、`LOG`、`DBG`。`__cplusplus` 分支在当前 C 构建中未启用，仅用于保持头文件的 C++ 链接兼容性。
- 静态扫描：
  - 全局变量：无。
  - 静态变量：无。
  - 静态函数：无。
  - 外部函数 `BspLed_Init`：在 `led.h` 声明，由 `app_led.c` 调用。
  - 外部函数 `BspLed_Write`：在 `led.h` 声明，由 `app_led.c` 两处调用。
  - 模块宏 `MOTOR_LED_H`：头文件保护宏，仅在 `led.h` 内使用，必须保留。
  - 调试属性命名：无 `DEBUG_`、`dbg_`、`debug_`、`test_`、`tmp_` 符号。
- 引用分析：
  - `BspLed_Init`：定义 1 处、头文件声明 1 处、外部调用 1 处。
  - `BspLed_Write`：定义 1 处、头文件声明 1 处、外部调用 2 处。
  - `LED_GPIO_Port`、`LED_Pin` 来自 `main.h`，均被本模块的 HAL GPIO 写操作使用。
  - 未发现只写不读变量、只读不写变量、未调用静态函数或仅在关闭的调试分支中使用的符号。
- 删除的变量/宏/函数（列表）：
  - 无。两个函数都是已被调用的公开板级接口；删除或内联到应用层会改变模块边界和对外接口。
- 合并/简化的逻辑：
  - 无。`BspLed_Init` 固定恢复 LED 初始电平，`BspLed_Write` 提供运行时电平设置，语义不同；互相转调不会产生可确认的代码或 RAM 减少。
- 保留但标记为“待确认”的项：
  - 无。
- 修改后代码行数变化：0 行（原 28 行 → 现 28 行）。
- 编译检查：通过。Keil Arm Compiler 6.24 全量构建为 0 errors、0 warnings。
- 行为回归检查：`tests/check_refactor.py` 在本模块修改前已失败（default case 0 的 init trace/events 不一致）；本模块无源码改动，因此该失败不是由本模块引入。
- 待办事项：
  - 无。

#### Diff

```diff
# 无源码改动
```
